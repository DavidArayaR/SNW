# =========================================
# SNW  -  Solo credenciales de la BASE DE DATOS
# =========================================
# Aquí solo van las variables DB_*, necesarias para conectarse a la base
# donde vive la tabla `configuracion`.

DB_HOST=127.0.0.1
DB_PUERTO=3306
DB_USUARIO=root
DB_CONTRASENA=
DB_NOMBRE=snw_base
